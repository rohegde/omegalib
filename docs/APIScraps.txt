// DATA TYPES
OMEvent
{
    uint id;
    enum type {TOUCH, MOVE, NEW, CLICK, ...}
    enum source {MULTITOUCH, TRACKER, COMPOSITE, ...)
    float x, y, z;
    // othet stuff.
}

OMState
{
    uint id;
    float x, y, z;
    // other stuff.
}

// APP SIDE API

void omegaInit(const char* cfg, int size);
void omegaDisplayFunction(func* func);
void omegaMainLoop();
int omegaGetEvents(OMEvent* ptr, int maxEvent, bool current);
OMState* omegaGetState(uint id);
int omegaGetNumEvents();
int omegaLostEvents();


// DRIVER SIDE API
driverAddStuffToEventList
driverAddStuffStateList


// IMPLEMENTATION INTERNAL
THREAD MANAGEMENT  (ot functions, othread.h olist.c)
LINKED LIST MANAGEMENT (ol functions, olist.h olist.c)
DISPLAY MANAGEMENT (od functions odisplay)
(LATER) ASYNCRONOUS EVENT MANAGEMENT

DRIVERS (od Functions)





